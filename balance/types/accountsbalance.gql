type Account {
  id: ID!
  balance: Float!
  availablebalance(context: String): Float!
}

type ReservedBalance {
  id: ID!
  context: String!
  balance: Float!
}

type VirtualBalance {
  id: ID!
  context: String!
  balance: Float!
}